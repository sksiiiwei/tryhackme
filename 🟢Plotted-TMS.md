### ДЛЯ РЕШЕНИЯ НЕОБХОДИМО
-
-
-
### ВЗЯТИЕ ШЕЛЛА
по классике начинаем со скана портов массканом и нмапом. видим аж два порта под веб-сервера и оба с заглушкой

<img width="697" height="334" alt="image" src="https://github.com/user-attachments/assets/276a9659-a3c8-46c5-9d13-8ac0c1dbc6b9" />

Делать нечего - фаззим оба сервера. На первом выдало несколько поддиректорий, типа /admin, /shadow - в них зашифрованный на base64 текст для прикольчика. но сервер 
на 445 порту наконец выдал полезную директорию - /managment. переходим по адресу ```http://ip:445/management``` и наконец видим сайт

<img width="855" height="565" alt="image" src="https://github.com/user-attachments/assets/6ab49d06-5072-4327-8f63-37f21ddac547" />

посмотрела код страниц - много мусора из hs скриптов и невызываемых нигде функций(причем уязвимых), типа

```$('#confirm_modal #confirm').attr('onclick',$func+"("+$params.join(',')+")")``` - уящвима к xss, хотя параметр парамс нигде на странице не определяется
```t = $src.split('.')[1]``` - некорректное определение расширения файла. при возможности подгрузки его можно было бы обойти чем-то вроде 'script.php.mp4'

но смысла особо в этом нет. из интересного - увидела, что в парметре f может передаваться login, flogin, slogin, update_settings

```
   $.ajax({
   url:_base_url_+'classes/Login.php?f=flogin',
   method:'POST',
   ...
   if(resp.status == 'success'){
      location.replace(_base_url_+'faculty');
```
то есть мы можем отдельно залогиниться за студента, факультет, админа (но форма логина на всех страницах никак не отличается), а также вызывать серверного обработчика для сохранения каких-то глобальных настроек. 
притом, как минимум на фронтенде, никаких препятствий для отправки любого файла. но это при условии, что у нас будет форма нужная форма - пока с этим мимо

```
  url:_base_url_+'classes/SystemSettings.php?f=update_settings',
    data: new FormData($(this)[0]),
```
Изучение кода на страницах сайта ничего конкретного не дало, кроме кучи потраченого времени, честно говоря, поэтому решила просто пофаззить и нашла поддиректорию /database
